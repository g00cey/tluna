#!/bin/bash
#create tmp directory (screen)
SCREEN_DIRECTORY='~/tmp'
REDHAT='/etc/redhat-release'
if [ -d $SCREEN_DIRECTORY ]; then
  echo 'already screen_directory'
else 
  mkdir ~/tmp
fi

#vim
ln -s ~/git/dotfiles/vim/vimrc        ~/.vimrc
cp    ~/git/dotfiles/vim/vimrc-color  ~/.vimrc-color
ln -s ~/git/dotfiles/vim              ~/.vim


#another dot files and w3m cgi-bin
rm -rf ~/.w3m
rm -rf ~/.gitconfig
rm -rf ~/cgi-bin
rm -rf ~/.irbrc
rm -rf ~/.screenrc


ln -s ~/git/dotfiles/w3m/             ~/.w3m
ln -s ~/git/dotfiles/gitconfig        ~/.gitconfig
ln -s ~/git/dotfiles/irbrc            ~/.irbrc
ln -s ~/git/dotfiles/cgi-bin/         ~/cgi-bin
ln -s ~/git/dotfiles/screenrc         ~/.screenrc
ln -s ~/git/dotfiles/bashrc           ~/.bashrc
ln -s ~/git/dotfiles/bash_profile     ~/.bash_profile

#git settings
git config --global user.name "t1una"
git config --global user.email "totugekiluna@gmail.com"
git config --global core.editor 'vim -c "set fenc=utf-8"'

#submoudle
cd git/dotfiles/vim
git submodule init
git submodule update
