#!/bin/bash

if [ -f /etc/redhat-release ] ; then
if [ ! -f /usr/bin/tmux ] ; then
bash ./centos-initial-install.txt
fi
fi

#delete links
rm  ~/.w3m
rm  ~/.gitconfig
rm  ~/cgi-bin
rm  ~/.irbrc
rm  ~/.bashrc
rm  ~/.bash_profile
rm  ~/.ctags
rm  ~/bin
rm  ~/.vimrc
rm  ~/.vim
rm  ~/.tmux.conf

#vim
ln -s ~/tluna/vim/vimrc        ~/.vimrc
ln -s ~/tluna/vim              ~/.vim

#another dot files and w3m cgi-bin

ln -s ~/tluna/w3m/             ~/.w3m
ln -s ~/tluna/gitconfig        ~/.gitconfig
ln -s ~/tluna/irbrc            ~/.irbrc
ln -s ~/tluna/cgi-bin/         ~/cgi-bin
ln -s ~/tluna/bashrc           ~/.bashrc
ln -s ~/tluna/tmux.conf        ~/.tmux.conf
ln -s ~/tluna/bash_profile     ~/.bash_profile
ln -s ~/tluna/ctags            ~/.ctags
ln -s ~/tluna/bin/             ~/bin

#git settings
git config --global user.name "t1una"
git config --global user.email "totugekiluna@gmail.com"
git config --global core.editor 'vim -c "set fenc=utf-8"'

#submoudle
git submodule init
git submodule update
